<Page x:Class="Devyatochka.Pages.SignUpPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Devyatochka.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SignUpPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="1" Grid.Column="1" MinWidth="250">

            <Label Content="Регистрация" FontSize="20" FontWeight="Bold" HorizontalContentAlignment="Center" Margin="0 0 0 20"/>

            <TextBlock Text="Логин:" Margin="0 0 0 5"/>
            <TextBox x:Name="textBoxLogin" Margin="0 0 0 10"/>

            <TextBlock Text="Пароль:" Margin="0 0 0 5"/>
            <PasswordBox x:Name="passwordBoxPassword" Margin="0 0 0 10"/>
            
            <TextBlock Text="Роль:" Margin="0 0 0 5"/>
            <ComboBox x:Name="comboBoxRole" Margin="0 0 0 20" DisplayMemberPath="Title" />

            <Button x:Name="buttonRegister" Content="Регистрация" Margin="0 5" Click="buttonRegister_Click"/>

        </StackPanel>
    </Grid>
</Page>
