<Page x:Class="Devyatochka.Pages.Admin.WeightTypeList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Devyatochka.Pages.Admin"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="WeightTypeList">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="LightGray" VerticalAlignment="Center">
            <Label Content="Название:" VerticalAlignment="Center" Margin="10 0 0 0"/>
            <TextBox x:Name="textBoxTitle" Width="150" VerticalAlignment="Center" Margin="5"/>

            <Button x:Name="buttonRefresh" Content="Обновить" VerticalAlignment="Center" Margin="10 5 5 5" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="wrapPanelContainer" Margin="5"/>
        </ScrollViewer>
    </Grid>
</Page>
